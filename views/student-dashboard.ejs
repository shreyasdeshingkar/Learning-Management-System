<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE-edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="./css/styles.css" />
    <title><%= title %></title>
</head>
<body>
    <div class="grid grid-cols-4">
        <div class="col-start-2 col-span-2">
            <%- include ('header.ejs') %>
            <div class="py-4 flex flex-col ml-6 mr-6 mt-3">
                <div class="py-2 border-b-2 m-2" style="display: flex; justify-content: space-between;">
                    <h1 class="text-black-900 text-xl font-semibold">Welcome, <br> Student: <%= currentUser.firstName %> <%= currentUser.lastName %> </h1>
                    <a href="/signout" class="text-green-600 text-xl font-semibold">Signout</a>
                </div>
                <div class="border-b-2 m-1">
                    <h1 class="text-xl font-bold text-orange-600 m-1">
                        Find Your Strength ; Your Learning Adventure Begins From here!!!
                    </h1>
                    <p class="text-medium text-gray-900 m-3">
                        Lets learn together! ; You are braver than you believe, stronger than you seem and smarter than you think
                    </p>                
                </div>
                <div class="m-2 flex flex-row justify-evenly bg-purple-100">
                    <a href="/MyCourses" class="bg-orange-600 text-white px-5 py-2 rounded font-medium hover:bg-orange-700 text-center block">
                        My Enrolled Courses
                    </a>
                    <a href="/changePassword" class="bg-blue-600 text-white px-5 py-2 rounded font-medium hover:bg-blue-700 text-center block">
                        Change Password
                    </a>
                </div>
                <hr class="m-3">
            </div>
            
            <!-- Display enrolled courses -->
            <%- include ('displayCourses.ejs', { currentUser, courses, users, enrols }) %>

            <% if(messages && messages.error && messages.error.length > 0)  { %>
                <% for(var i=0; i<messages.error.length; i++) { %>
                    <p class="bg-red-300 my-4 list-none rounded border-gray-50 px-4 py-2"><%= messages.error[i] %></p>
                <% } %>
            <% } %>
        </div>
    </div>
</body>
</html>